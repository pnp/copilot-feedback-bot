

export enum ChartView {
  OneWeek,
  OneMonth,
  ThreeMonths,
  SixMonths,
  OneYear
}

export enum LogicalOperator
{
    Unknown = 0,
    Equals = 1,
    NotEquals = 2,
    GreaterThan = 3,
    LessThan = 4,
}

export enum QuestionDatatype {
  Unknown = 0,
  String = 1,
  Int = 2,
  Bool = 3,
}
